# Issue #112 — status

- [x] Done

## What's done

- Added `_as_quantity`, `_coerce_scale`, `_resolve_units_spec` in `converters.py`.
- Widen `get_converter_to_specific`, `nominal_capacity_as_absolute`,
  `calculate_nom_cap_abs_from_specific`, `calculate_specific_conversion_factors`
  (quantity strings, per-param unit kwargs, bulk `cellpy_units=`).
- Docs: defaults table + three usage patterns in `standalone-use.md`.
- Tests: quantity strings, unit kwargs, data-object paths; 28 unit tests green.
- `HISTORY.md` unreleased bullet.

## Remaining work

- None. Ready for `/iflow-close`.
